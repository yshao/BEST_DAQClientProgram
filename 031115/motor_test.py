import glob

self.send('')
ln=sender.getText()
mem=ln.splitlines()

for l in mem:
    self.send(ln)
    logger


def send(self,ln):
    self.logger.append(ln)
    self.client.send(ln)


### sort into three files ###
local=cfg['local_dir']
ext='imu'
glob.glob('%s/*.%s'%(local,ext))

ext='enc'
glob.glob('%s/*.%s'%(local,ext))

ext='rad'
glob.glob('%s/*.%s'%(local,ext))


### ftp ###
